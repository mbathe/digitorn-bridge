meta {
  name: stop_watcher
  type: http
  seq: 141
}

delete {
  url: {{base_url}}/api/apps/{{app_id}}/watchers/{{watcher_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Stop and remove a watcher.
}
