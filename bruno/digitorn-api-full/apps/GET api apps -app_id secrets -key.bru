meta {
  name: check_secret
  type: http
  seq: 40
}

get {
  url: {{base_url}}/api/apps/{{app_id}}/secrets/{{key}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Check if a secret exists (value is never returned).
}
