meta {
  name: preview_server_logs
  type: http
  seq: 32
}

get {
  url: {{base_url}}/api/apps/{{app_id}}/preview-server/logs
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:query {
  limit: 
}

docs {
  Return the last ``limit`` log lines captured from the dev server.
}
