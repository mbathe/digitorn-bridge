meta {
  name: get_watcher
  type: http
  seq: 68
}

get {
  url: {{base_url}}/api/apps/{{app_id}}/watchers/{{watcher_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Get watcher status, metrics, and recent history.
}
