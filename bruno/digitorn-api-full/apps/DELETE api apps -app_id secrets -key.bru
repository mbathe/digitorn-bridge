meta {
  name: delete_secret
  type: http
  seq: 138
}

delete {
  url: {{base_url}}/api/apps/{{app_id}}/secrets/{{key}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Delete a secret.
}
