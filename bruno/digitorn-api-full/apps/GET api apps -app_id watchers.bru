meta {
  name: list_watchers
  type: http
  seq: 67
}

get {
  url: {{base_url}}/api/apps/{{app_id}}/watchers
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  List all watchers (running + paused).
}
