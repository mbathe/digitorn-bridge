meta {
  name: list_secrets
  type: http
  seq: 39
}

get {
  url: {{base_url}}/api/apps/{{app_id}}/secrets
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  List secret key names for an app (values are never returned).
}
