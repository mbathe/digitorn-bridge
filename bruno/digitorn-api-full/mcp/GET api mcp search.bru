meta {
  name: search_servers
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/mcp/search
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:query {
  q: 
}

docs {
  Search for MCP servers in catalog + registry.
}
