meta {
  name: pool_connect
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/mcp/pool/{{mcp_server_id}}/connect
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Manually connect a server. **Admin-only.**
}
