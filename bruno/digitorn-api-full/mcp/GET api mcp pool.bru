meta {
  name: pool_status
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/mcp/pool
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Show the live daemon MCP pool status.
}
