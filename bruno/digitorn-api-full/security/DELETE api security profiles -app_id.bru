meta {
  name: delete_profile
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/api/security/profiles/{{app_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Delete a security profile and all its module grants.
}
