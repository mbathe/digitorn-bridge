meta {
  name: api_metrics_session
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/metrics/sessions/{{session_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:query {
  app_id: 
}

