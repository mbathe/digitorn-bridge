[provider]
name         = "gcp"
display_name = "Google Cloud (Service Account)"
handler_type = "gcp_service_account"
icon         = "gcp"
category     = "cloud"
description  = "GCP service account JSON key. Validated by JSON shape."
homepage     = "https://console.cloud.google.com/iam-admin/serviceaccounts"

[[fields]]
name = "service_account_json"
help = "Download from the GCP console: IAM -> Service Accounts -> Keys -> Add key (JSON)."
