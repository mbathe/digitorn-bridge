[module]
module_id = "web_preview"
module_class_path = "digitorn.modules.web_preview.module:WebPreviewModule"
version = "1.0.0"
description = "Session-scoped iframe preview attachments — the LLM points the /preview/ endpoint at a running dev server (proxy) or a built static dir (static), with multi-attach by name."
author = "Digitorn Team"
isolation = "shared"
platforms = ["all"]
requirements = []
tags = ["preview", "web", "iframe", "session"]
