mcp:
  servers:
    github:
      env:
        GITHUB_TOKEN: "{{env.GITHUB_TOKEN}}"
    slack:
      env:
        SLACK_TOKEN: "{{env.SLACK_TOKEN}}"
  config:
    cache:
      scope: auto
      ttl: 300
