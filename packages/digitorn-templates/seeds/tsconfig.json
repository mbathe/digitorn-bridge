{
  "//": "Language-server only — these .tsx files are not compiled by the package's tsc build. They ship as raw source and are bundled by the consumer's Vite plugin (@digitorn/preview-sdk/vite). This tsconfig only tells the IDE which JSX runtime / lib / target to type-check seeds against, so we don't get red squiggles on perfectly valid seed source.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "lib": ["DOM", "DOM.Iterable", "ES2022"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "types": []
  },
  "include": ["**/*.tsx", "**/*.ts"],
  "exclude": ["node_modules"]
}
