app:
  app_id: channels-29-multi-filter
  name: Multi Filter AND
runtime:
  mode: background
agents:
- id: main
  brain:
    provider: deepseek
    model: deepseek-chat
    backend: openai_compat
    config:
      base_url: https://api.deepseek.com/v1
      api_key: '{{env.DEEPSEEK_API_KEY}}'
  role: Handle critical new tickets only. Acknowledge and suggest immediate resolution
    steps.
tools:
  modules:
    channels:
      config:
        providers:
          tickets:
            adapter: webhook
            config:
              inbound_path: /hook/tickets
            activation:
              filter:
              - field: event.payload.status
                equals: new
              - field: event.payload.priority
                lt: 3
              message: 'High-priority new ticket: {{event.payload.title}} (P{{event.payload.priority}})'
